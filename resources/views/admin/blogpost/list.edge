@layout('admin-master')

@section('title')
    <title>
        All Blog Posts | Singh & Singh
    </title>
@endsection

@section('extra_style')
    <style>
        .heading{
            display: flex;
            align-items: center;
            justify-content: center;
            margin-top: 1em;
        }
    </style>
@endsection

@section('content')
    <div class="container">
        <div class="heading">
            <h3 class="mr-3">Blog Posts</h3>
            <a href="{{ route( 'blogpost.add_page' ) }}" class="btn btn-success">&plus; New Post</a>
        </div>
        <hr> 
        <div class="post-list row">
            @each(post in blogposts)
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <img 
                            src="{{ post.image_url }}" 
                            class="card-img-top"
                            style="height:200px;object-fit:cover;object-position:center;"
                        >
                        <div class="card-body">
                            <h5 class="card-title">{{ post.title }}</h5>
                            <a href="{{ route( 'blogpost.details',{id:post.id} ) }}" class="btn btn-primary">View</a>
                        </div>
                    </div>
                </div>
            @else
                <h4 class="alert alert-warning text-center col-md-12">
                    No Blog Posts Yet !
                </h4>
            @endeach
        </div>
    </div>
@endsection